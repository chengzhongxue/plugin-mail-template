# Changelog

## 1.2.0

- 将运行基线升级至 Halo 2.26、Java 21 和 Gradle 9.4。
- 将 Console 前端迁移到 Halo 官方 Vite 插件打包工具、Vue 3.5 和 TypeScript 6。
- 新增服务端 Thymeleaf 模板校验端点，保存与测试发送前返回可读语法错误。
- 保存时为现有的每个语言键创建 Halo 模板选择器可确认命中的最新版本，并清理旧的 `template-one-*` 版本，避免本地化模板覆盖刚保存内容。
- 重写测试发送流程，使用刚保存模板的身份并按通知类型串行化插件操作，避免并发误测、异步 Reason 与临时订阅之间的竞态，并保证失败或取消时清理。
- 新增待处理 Reason 检查；默认阻止可能触发历史通知补发的保存，管理员看到数量并显式确认后才允许继续。
- 保留 `NotificationTemplateReconciler` 的升级保护：内置模板重新创建后，按语言恢复自定义模板的选择优先级，完整保留标题、HTML 和纯文本；新副本确认生效后才清理旧副本，并与保存、恢复操作共用锁。
- 新增 HTML/纯文本双编辑器、统一保存/测试/恢复操作锁、切换/离开防丢失保护、移动端双面板布局和更安全的 iframe 预览。
- 新增最小权限 RBAC 角色模板、Java 单元测试、依赖审计和完整前端质量检查。
